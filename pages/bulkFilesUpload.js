import { ContentLayout } from '../components/ContentLayout';
import { Layout } from '../components/Layout';

const BulkFilesUploadPage = () => {
  return (
    <Layout>
      <ContentLayout header="Bulk Files Upload">bulk</ContentLayout>
    </Layout>
  );
};

export default BulkFilesUploadPage;
